import { Button } from 'antd-mobile';
import './footer.scss';

export const Footer = () => {

    return <footer className="footer">
        <Button color='success' style={{ borderRadius: 'none' }}>Begin</Button>
    </footer>
}
